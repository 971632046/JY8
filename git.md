### git的使用
----
```git init //初始化  
git add [文件名]  
git commit -m "[描述的内容]"  
git remote add origin https://github.com/971632046/JY8.git  
git push -u origin master  
------
git remote remove origin  
git remote add origin [网络地址（与上面的相同）
//修改远程地址  
```
-----
```
git init 
git add note.md
git commit -m "wowudi"  
git remote add origin https://github.com/971632046/JY8.git
git push -u origin master
git remote remove origin
git remote add origin
```

